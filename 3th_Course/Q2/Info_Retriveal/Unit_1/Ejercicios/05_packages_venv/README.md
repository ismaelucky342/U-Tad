# Ejercicios 05 — Paquetes, pip y entornos virtuales

## Checklist (TO‑DO)
- [ ] Crear y activar `.venv`
- [ ] Instalar dependencias dentro del entorno
- [ ] Generar `requirements.txt`
- [ ] Entender `pip install -r` y reproducibilidad
- [ ] (Opcional) instalar un paquete en editable `-e`

## Ejercicio 05.1 — requirements
1. Instala `pandas`.
2. Crea un `requirements.txt` en `Unit_1/`.
3. Borra el venv, recréalo e instala con `pip install -r requirements.txt`.

## Ejercicio 05.2 — Paquete local (opcional)
Crea una carpeta `my_ir_utils/` con un `__init__.py` y una función `normalize`.
Instálalo en editable: `pip install -e .`
